export default function MainContent() {
  return (
    <main className="bg-body-tertiary py-3 mt-4">
      <ol>
        <li>A React component file is a self-contained, modular unit of code (typically a .jsx or .tsx file) that encapsulates the structure (HTML-like JSX), logic (JavaScript), and sometimes styling for a specific part of the user interface.</li>
        <li>Components allow developers to build UIs by combining independent, reusable building blocks that can be nested and managed in isolation.</li>
      </ol>
    </main>
  );
}